cache:
  paths:
    - web/app/themes/casey/node_modules/

build_stage:
  script:
    - composer install
    - cd web/app/themes/casey
    - composer install
    - npm install
    - yarn run build:production
    - cd ../../../../
    - rsync -t * /srv/www/caseydris.co/stage_html/
  stage: build
  only:
    - stage
